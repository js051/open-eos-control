# Feature Status And Acceptance

Last audited: 2026-08-09.

This is the canonical completeness ledger for Open EOS Control. A UI control, interface method, or simulator response alone does not make a feature complete.

## Status Rules

- **Implemented**: an executable product path and automated coverage both exist.
- **Device validation**: implemented and test-covered, but still needs a recorded result from the named physical camera.
- **Planned**: architecture or interface only; users must not be shown an active product control.
- **Research**: public platform or protocol evidence is not yet sufficient for a product claim.
- **Supported** in a diagnostic report means the camera or active engine advertised a usable path; it is not proof that the path succeeded on that device.
- **Observed this session** means the current client received a successful response or valid payload from the real backend. Failed, rejected, and merely advertised operations are never added.

## Android And Canon CCAPI

| Capability | Code status | EOS R6 Mark III | Acceptance evidence |
| --- | --- | --- | --- |
| Discovery and API versions | Implemented | Device validation passed; refreshed capability report required | Android, iOS, and PC contract tests cover same-origin Canon `url` and relative `path` entries plus Canon's exact `No list of APIs` response and `/ccapi/ver100/topurlfordev` fallback. Cross-origin/unsafe entries are ignored, failed developer-list requests do not invent capabilities, and each client records a bounded structural trace for root, developer-list and identity attempts; [sanitized physical-camera record](validation/eos-r6-mark-iii-android-ccapi.md) |
| Capability evidence diagnostics | Implemented | Fresh device report required | Android, iOS and Desktop Bridge reports expose a report schema, UTC generation time, installed version, bounded discovery source, protocol versions, advertised method/path pairs, writable setting keys, success-only `observedFeatures`, advertised/observed differences, and a cross-platform discovery trace. Each attempt contains only a fixed relative endpoint, outcome, optional HTTP status, sanitized top-level response keys, protocol versions and valid operation count; raw JSON, response values, origin, credentials, Authorization, exception text and local paths are never copied into the trace. Android, iOS and the PC control UI also offer a session-scoped physical-validation checklist only for advertised-and-observed features; Simulator/Offline Preview are rejected, confirmations clear across sessions, and copied Markdown omits serials, URLs, endpoints, raw status, credentials and local paths while retaining a native SHA-256 link to the matching sanitized diagnostic. Credentials and physical camera serials are redacted before display/copy; deterministic Android, Swift, Python and JavaScript tests plus mobile and browser UI tests cover the report contract. |
| Identity, battery, storage | Implemented | Identity and battery passed; structured storage summary requires a refreshed device report | CCAPI, Android USB/PTP and Desktop Bridge tests preserve availability, device count, total/free bytes and remaining shots; Android, iOS and Bridge classify normalized R6 Mark III aliases into the same canonical `EOS_R` / `PRIMARY` profile; shooting HUDs, Debug views and diagnostic reports consume the same status fields without inferring missing values |
| CCAPI recordable information | Implemented when advertised | R6 Mark III endpoint availability and Photo/Video transitions require a refreshed device report | Android, iOS and PC read only an advertised `GET /shooting/information/recordable`. Both Canon fields must exist and be either null or a nonnegative integer; malformed payloads do not create capability evidence. Photo HUDs prefer exact recordable shots, Video HUDs show exact remaining seconds as a duration, and storage remains an explicit fallback. Direct-client, Bridge, Simulator, mobile UI and browser end-to-end tests cover valid, malformed and 2,418 to 2,417 shot transitions plus 7,200 seconds of recording time. |
| CCAPI lens and temperature status | Implemented when advertised | R6 Mark III endpoint availability and restriction transitions require a refreshed device report | Android, iOS and PC read only advertised `GET /devicestatus/lens` and `GET /devicestatus/temperature`. Exact Canon boolean/string lens payloads and the 12 documented temperature tokens are required before the capability becomes supported. Localized control warnings and diagnostics expose valid state; still capture, recording start and Live View start refresh the restriction immediately before sending a command, while recording, Bulb and Live View stop/release paths remain available. Direct-client, Bridge and Simulator tests assert valid, malformed, restricted-start and cleanup behavior. |
| CCAPI event polling | Implemented when advertised | R6 Mark III same-version GET/DELETE advertisement and sustained external-change refresh require a refreshed device report | Android, iOS and PC require both `GET` and `DELETE /event/polling` in one advertised API version. Version 1.0 uses `continue=on`; version 1.1+ uses `timeout=long`. Responses are bounded change hints only, so non-empty events trigger authoritative status/capability refresh and disconnect sends DELETE cleanup. Direct clients, Bridge, Simulator and Android/iOS/PC end-to-end tests cover body-side ISO/media changes, stale refresh ordering and cleanup; a physical latest-firmware session is still required. |
| ISO, Tv, Av, WB | Implemented | Device validation passed | Advertised setting paths, unit tests, physical camera control |
| Dynamic camera settings | Implemented | Device validation in progress | Scalar values come from camera `ability`; controls require an advertised setting-specific `PUT` path. Canon's object-valued `stillimagequality` exposes RAW/JPEG/HEIF controls and `wbshift` exposes bounded B/A and M/G axes on Android, iOS and PC. Both preserve companion fields and write the exact nested `value` object; image quality rejects an all-disabled result and WB shift rejects malformed, oversized, off-step or out-of-range values before HTTP. |
| CCAPI camera beep and display-off timeout | Implemented when advertised | R6 Mark III endpoint availability, ability values and write transitions require device validation | Android, iOS and PC independently require exact same-version `GET`/`PUT /functions/beep` and `/functions/displayoff` pairs. Beep accepts only `enable`, `disable`, or `disabletouch`; display timeout accepts only the documented second strings `10`, `20`, `30`, `60`, `120`, or `180`. Each control needs at least two unique values containing the current value, and every write re-reads both advertised resources before sending the exact string. Canon 400 and busy/shooting/recording 503 responses remain real failures. Direct-client, Bridge, Simulator, localization, Photo/Video mode-filter, offline-preview, Android Compose/Simulator and PC browser end-to-end tests cover valid, malformed, stale and cross-version contracts. Immediate auto-power-off is intentionally not represented by this setting. |
| CCAPI optical zoom | Implemented when advertised | R6 Mark III endpoint availability requires device validation | Android, iOS and PC require matching same-version `GET` and `POST /shooting/control/zoom`, validate Canon's integer current/min/max/step contract, cap generated choices at 256, and send an integer POST only after slider release. This is distinct from Live View magnification and USB power-zoom speed; the control stays hidden when the camera/lens combination does not advertise a usable endpoint. |
| CCAPI sound recording level | Implemented when advertised | R6 Mark III internal-microphone transition passed; external/accessory sources still require attached-hardware validation | Android, iOS and PC support the legacy level resource plus independently advertised `level/intmic`, `level/extmic`, and `level/acc` same-version GET/PUT pairs. Each requires an exact integer current value inside a bounded integer min/max/positive-step range of 2-256 choices and is re-read before every integer write. On R6 Mark III firmware 1.1.0, switching the internal microphone from Auto to Manual exposed 64 levels; an adjacent write, readback and restore passed. Absent-source and mode-dependent 503 responses remain real failures. |
| CCAPI sound recording mode, wind filter and attenuator | Implemented when advertised | R6 Mark III overall recording, internal-microphone mode and wind-filter writes passed; attached-source controls remain pending | Android, iOS and PC support the legacy resources plus independently advertised `mode`, `windfilter`, and `attenuator` children for `intmic`, `extmic`, and `acc`. A control requires at least two unique bounded documented strings containing the current value; one-value abilities and unavailable-source 503 responses remain hidden. R6 Mark III firmware 1.1.0 accepted and restored overall recording, internal-microphone Auto/Manual, and internal wind-filter transitions through the production Bridge. |
| CCAPI focus bracketing | Implemented when advertised | R6 Mark III endpoint availability, range values and Photo-mode writes require device validation | Android, iOS and PC first require an exact same-version `GET`/`PUT /shooting/settings/focusbracketing` pair with the documented `enable`/`disable` strings. Only then can the independently advertised number-of-shots, focus-increment and exposure-smoothing child resources become controls. Integer current/min/max/positive-step contracts are exact and bounded to at most 1,024 generated values; every write re-reads all advertised focus-bracketing resources before sending an integer or exact string. The group is Photo-only, with large ranges rendered as sliders. Canon busy, shooting/recording and Movie-mode 503 responses remain real failures. Direct-client, Bridge, Simulator, localization, mode-filter, offline-preview and PC browser end-to-end tests cover valid, malformed, stale and cross-version contracts. |
| CCAPI movie quality, high frame rate, cropping and format | Implemented when advertised | R6 Mark III quality, cropping and current XF format writes passed; high-frame-rate remains mode-dependent | Android, iOS and PC independently require exact same-version GET/PUT pairs for `moviequality`, `highframerate`, `moviecropping`, and `movieformat`. Each control needs at least two unique bounded camera-advertised strings containing the current value. Toggle tokens remain strict; quality and format stay forward-compatible with camera-advertised values. R6 Mark III firmware 1.1.0 exposed five `raw`/XF-HEVC S/XF-AVC S formats and accepted an adjacent format write plus exact restore through the production Bridge. |
| CCAPI dual-card selection | Implemented when advertised | R6 Mark III endpoint availability, slot mapping and body transition require device validation | Android, iOS and PC independently pair same-version `GET`/`PUT /functions/cardselection/stillimage` and `/functions/cardselection/movie`. A control requires a string current value inside a unique camera-advertised list of at least two exact `none`/`card1`/`card2` values. Photo and Video filter the controls into their respective settings; unadvertised, malformed, stale and cross-version values never produce a write. Direct-client, Simulator, localization, mode-filter and PC browser end-to-end tests cover the contract. |
| CCAPI capture directory | Implemented when advertised | R6 Mark III endpoint availability, creation and selection transitions require device validation | Android, iOS and PC require one advertised API version to contain `POST /functions/directory/createdirectory` and matching `GET`/`PUT /functions/directory/directoryselection`. The selection response must contain a unique bounded list of one to 256 eight-character Canon directory values and its current value, so a newly formatted single-directory card still exposes creation. Creation accepts only empty or exactly five ASCII uppercase letters, digits or underscores, validates the returned five-character name, and refreshes the advertised selection list. More Settings exposes creation only for the complete capability; selection uses the ordinary Photo-only setting control. Direct-client, Bridge REST/client, Simulator, offline-preview and Android UI tests cover valid, malformed and cross-version contracts. Physical R6 Mark III behavior on the installed firmware remains required. |
| CCAPI file naming | Implemented when advertised | R6 Mark III endpoint availability and all seven field transitions require device validation | Android, iOS and PC require one API version to advertise GET and PUT for all seven `/functions/filename` resources. Still mode accepts only `preset_code`, `usersetting1` or `usersetting2`; the two still prefixes, movie index and five-character movie name use Canon's exact uppercase ASCII lengths; reel 1-9,999 and clip 1-999 are strict integers inside camera-reported positive-step ranges. Writes use each endpoint's documented key and type, then require a valid complete readback before success. Photo and Video show only their relevant fields; unsupported, incomplete, malformed and cross-version groups stay hidden. Direct Android/iOS/Bridge clients, Simulator, Android Compose and PC Playwright tests cover the contract, including background-event-safe PC input drafts. USB/PTP and libgphoto2 do not claim an inferred equivalent. |
| CCAPI Movie Mode | Implemented when advertised | R6 Mark III endpoint availability and body transition require device validation | Android, iOS and PC require matching same-version `GET` and `POST /shooting/control/moviemode`, accept only Canon's `status=on|off`, and send `{"action":"on|off"}` from the Photo/Video selector. The control stays local-only when the endpoint pair or response is invalid, failed writes retain the last camera-confirmed mode, and recording remains independently gated by `recbutton`. Simulator, direct-client, ViewModel/SwiftUI and browser end-to-end tests assert the exact camera-side state. |
| CCAPI Auto Power Off and camera sleep | Implemented when advertised | R6 Mark III endpoint values, timed writes and immediate sleep require device validation | Android, iOS and PC require an exact same-version `GET`/`PUT /functions/autopoweroff` pair. Timed settings accept only documented `30`/`60`/`120`/`180`/`300`/`600`/`disable` strings from a complete live ability with at least two normal choices, then re-read before every exact write. `immediately` is never a selectable setting; it enables a separate confirmation-gated `CAMERA_SLEEP` action only when explicitly advertised. Successful HTTP 202 acceptance stops local Live View/event/media work and disconnects the session. Direct-client, Bridge, Simulator, localization, disabled-preview and PC browser end-to-end tests cover valid, malformed, stale, timed-only, busy and disconnect behavior. libgphoto2 and Android USB expose only their separately proven timed controls. |
| CCAPI sensor cleaning | Implemented when advertised | R6 Mark III firmware 1.1.0 endpoint and physical cleaning cycle require device validation | Android, iOS and PC expose a confirmation-gated maintenance action only for an exact advertised `POST /functions/sensorcleaning`. They send Canon's strict `{"autopoweroff":<boolean>}` body, require the documented HTTP 200 response, stop event polling and Live View before the command, then either refresh and restore the session or disconnect when auto power-off was selected. Recording, Bulb, preview and conflicting operations keep the action disabled; rejected responses never become observed evidence. Direct-client, Bridge, Simulator, localized UI, offline-preview and browser end-to-end tests cover advertised, unadvertised, invalid, busy and noncanonical-success behavior. libgphoto2 and Android USB remain unsupported because no verified public command contract has been established. |
| Media protection, archive, rating and display rotation | Implemented when advertised | R6 Mark III CCAPI contents PUT, USB `0x1012`, and MTP rating-property advertisement plus physical file transitions require device validation | Android, iOS and PC CCAPI require an advertised Canon contents `PUT`, accept only documented protection/archive booleans, rating 0-5 and rotation 0/90/180/270 values, send the exact action/value body, and perform up to three bounded `kind=info` readbacks before success or observed evidence. Android direct USB separately exposes protection when DeviceInfo advertises the complete media-browser path and standard PTP `SetObjectProtection (0x1012)`, and rating only when the complete standard MTP object-property operation set, a concrete card-media format's writable UINT16 Rating descriptor and all six `0/20/40/60/80/100` wire values are proven. Both paths require exact same-handle readback; unknown values and app-private Phone captures remain non-writable. Simulator, direct clients, Android PTP/session/backend/Compose, Swift clients and PC tests cover persistence, invalid input, unadvertised commands, rejected or mismatched readback and UI dispatch. USB archive/rotation and all libgphoto2 media metadata writes remain planned/unsupported. |
| JPEG Live View | Implemented | Device validation passed at requested 15 FPS | Frame parser tests and [physical rolling-FPS record](validation/eos-r6-mark-iii-android-ccapi.md) |
| CCAPI multipart JPEG Live View | Implemented when advertised | R6 Mark III first-frame and POST-off lifecycle passed through the integrated PC engine on 2026-08-11; Android/iOS and sustained 6/15/30 FPS remain | Android, iOS and PC require a same-version general POST start, multipart GET/DELETE, and either a matching general DELETE or Canon's POST-off general stop. They validate the multipart boundary, bounded per-part headers and complete JPEG data, continuously drain the stream while keeping only the newest frame, treat a rejected multipart DELETE as best-effort while still executing the general stop, and mark observation only after a valid frame. AUTO tries RTP, multipart, then JPEG polling; explicit sources never silently change. |
| Android Live View monitoring assists | Implemented for decoded frames | Device validation required for sustained-rate performance | A memory-only imported 3D `.cube` LUT is applied by one conflated worker that keeps only the newest decoded JPEG/USB/Bridge frame. A bounded 120x80 post-LUT analysis path drives a mutually exclusive luminance histogram or 64x64 luma waveform, configurable zebra, false color and focus peaking. Frame guides, action/title safe areas and 1.33x/1.5x/1.8x/2x anamorphic desqueeze are geometry-based. Native RTP retains its zero-copy surface, so LUT and pixel analysis are explicitly disabled there while guides/desqueeze remain available. Parser, interpolation, analysis and Compose interaction/rendering tests cover the contract. |
| Android capture-mode and exposure interaction | Implemented | Device validation required for control latency and rejected writes | A fixed 48dp Photo/Video selector keeps capture context visible beside the shutter and is disabled during recording, Bulb or conflicting writes. It changes the body mode only through a camera-advertised writable shooting-mode value and restores only an observed photo mode. Exposure dials snap between advertised values, prevent input while a write is active, and realign to the returned camera value after success or failure. Unit and Compose tests cover selection, locking and rollback; Pixel API 34 and Android 16/API 36 AVD jobs drive the production UI, ViewModel, repository and HTTP client against the running Simulator. The Simulator-preset workflow verifies decoded Live View, ISO, still/movie capture, Tap AF, Click WB, balanced AF-ON and half-press, focus drive, mode-gated Bulb, decoded media preview and confirmed deletion. A separate HTTP-preset workflow explicitly overrides localhost simulation detection and requires Canon `/ccapi` discovery, versioned ISO/capture/JPEG endpoints, Canon 1.1 event polling, camera-side ISO synchronization without manual refresh, and disconnect cleanup. Both jobs also retain system rotation-lock and orientation-aware English/Traditional Chinese evidence. Simulator state is asserted after each command and remains deterministic evidence only, not physical-camera acceptance. |
| iOS Live View monitoring assists | Implemented for decoded frames | Device validation required for sustained-rate performance | The SwiftUI app applies a memory-only imported 3D `.cube` LUT with Core Image `CIColorCube` through one conflated worker, then runs the same bounded 120x80 histogram/luma-waveform, configurable zebra, false color and focus peaking analysis on decoded JPEG/Bridge frames. Frame guides, action/title safe areas and anamorphic desqueeze are geometry-based. Native RTP keeps sample-buffer rendering, so LUT and pixel analysis are disabled there while guides/desqueeze remain available. Parser/rendering unit tests and iPhone Simulator interaction tests cover the contract. |
| PC Live View monitoring assists | Implemented for decoded frames | Device validation required for sustained-rate performance | The browser control UI applies a memory-only imported 3D `.cube` LUT with a WebGL2 3D texture, then analyzes a bounded 120x80 copy of each decoded Bridge or local UVC/HDMI frame. Mutually exclusive histogram/luma-waveform scopes, configurable zebra, false color, focus peaking, frame guides, safe areas and anamorphic desqueeze are localized. Diagnostics expose only LUT loaded state and cube size, never its filename, title or table. Parser/interpolation tests, static contract tests and desktop/narrow browser rendering checks cover the implementation. |
| Still capture | Implemented | Device validation required | Direct shutter requires advertised POST; manual PUT/POST flow always releases |
| Bulb long exposure | Implemented when manual press/release is advertised | Device validation required | Android, iOS and PC use Canon's manual `full_press` with `af=false`, keep the last Live View frame without active JPEG polling during exposure, and send `release` before resuming. The central shutter appears only in an advertised Bulb shooting mode; stop remains retryable after a release failure, disconnect performs best-effort release, and success is observed only after release completes. |
| Independent AF-ON | Implemented when advertised or a verified half-press path exists | Device validation required | Canon sample-backed CCAPI `POST /shooting/control/af` sends `action=start`, waits briefly, and guarantees `action=stop`; Android USB prefers advertised `DoAf (0x9154)`/`AfCancel (0x9160)` with non-cancellable cleanup; libgphoto2 prefers writable runtime `autofocusdrive`/`autofocuscancel`; both USB paths fall back to balanced half-press |
| Shutter half-press | Implemented when advertised | Device validation required | Advertised manual shutter endpoint, timed half-press, guaranteed release, capability-gated Android More Settings, PC quick action and iOS More menu, plus positive/negative UI coverage |
| Movie recording start/stop | Implemented | Device validation required | Control is enabled only when `recbutton` is advertised; entering or leaving Movie Mode is the separate capability above |
| Tap focus | Implemented when advertised | Device validation required | Requires advertised `PUT afframeposition` plus `flipdetail`; bounded Canon detail packets provide image geometry, normalized UI taps are converted to integer `positionx`/`positiony`, and missing metadata sends no guessed command |
| Click White Balance | Implemented when advertised | Device validation required | Requires advertised `POST clickwb` plus `flipdetail`; uses the same bounded Canon image geometry as Tap AF, exposes an explicit tap-action selector, and sends no guessed command when metadata is unavailable |
| Media browser | Implemented | Device validation required | Bounded storage-tree and page traversal; same-origin path validation; unit tests |
| Media thumbnails | Implemented | Device validation required | Canon's CAP overview and pinned official Android sample define `?kind=thumbnail` on returned content URLs. Android, iOS and PC direct CCAPI use same-origin structured queries, an 8 MiB bound, image MIME/signature checks and UI decode fallback; Simulator and contract tests exercise the exact request |
| Media display preview | Implemented for CCAPI JPEG/CR3 candidates and wired JPEG/PNG items | Device validation required | Canon CCAPI Reference 1.3 defines `?kind=display` under the advertised contents GET and limits it to JPEG/CR3, with additional per-file rejection cases. Android USB/PTP uses advertised standard `GetObject`; libgphoto2 uses `--get-file ... --stdout` for camera media and bounded local reads for host captures. Every item carries `previewAvailable`; direct CCAPI validates each returned representation, while wired paths accept only complete JPEG/PNG data up to 32 MiB. Android, iOS and PC clients consume the same gate. |
| Media download | Implemented as a streaming transfer | Device validation required | Android uses a SAF destination; iOS uses a file-backed URLSession transfer; PC consumes the authenticated Fetch body incrementally with byte progress/cancel, a cross-browser Blob fallback for ordinary files, and temporary-file-backed direct writes for unknown or 64 MiB-plus files when File System Access is available. Cancellation and teardown avoid success reporting and discard incomplete direct writes. |
| Media upload | Implemented when the wired engine advertises a writable destination | R6 Mark III USB upload and readback require device validation | Android first snapshots every SAF source into an app-private temporary file, then requires standard `SendObjectInfo (0x100C)` and `SendObject (0x100D)`, a writable card, a matching camera-advertised image/video format, exact bytes up to the UINT32 USB-container limit minus its 12-byte header, and same-handle `GetObjectInfo` readback. A failed/cancelled USB data phase closes the poisoned PTP transport and requires reconnect; once the camera accepts `SendObject`, readback and list verification finish non-cancellably. Desktop Bridge/libgphoto2 requires runtime File Upload evidence plus writable storage, stages a bounded raw request, rejects case-insensitive card-name collisions, invokes `--folder`/`--filename`/`--upload-file`, terminates that subprocess when the client disconnects before commit, and always verifies one fresh case-insensitive name/exact-size match after process success. Android, iOS, and PC reconcile an ambiguous late cancellation through a fresh media list, expose picker/progress/cancel only for `MEDIA_UPLOAD`, refresh after success, and never claim a direct CCAPI upload endpoint. Simulator contract tests preserve exact bytes and enforce production filename/MIME categories plus atomic duplicate insertion. |
| Media deletion | Implemented when advertised | Device validation required | Same-origin exact resource `DELETE`, confirmation UI, success-only list mutation, simulator and unit/UI tests |
| CCAPI RTP H.264/AAC LATM Live View on Android | Implemented when advertised | Device revalidation required; the 2026-08-11 R6 Mark III camera-Wi-Fi probe advertised RTP but rejected start with HTTP 503 in both tested movie-mode states | Requires advertised `GET rtpsessiondesc` plus `POST rtp`, a reachable camera-Wi-Fi IPv4 address, SDP validation, Wi-Fi-bound UDP reception, RFC 3550/RFC 6184 H.264 packet handling and native `MediaCodec` rendering. Startup succeeds only after a bounded decodable key frame with SPS/PPS; bounded SDP `sprop-parameter-sets` seed parameter sets when the stream does not repeat them. A five-second no-video timeout reports UDP datagram/access-unit/keyframe evidence, performs exact receiver and camera stop cleanup, enables AUTO fallback, and leaves explicit RTP as a readable error. When SDP separately advertises in-band `MP4A-LATM/48000`, Android performs bounded RFC 6416 reassembly/loss recovery, extracts raw AAC with Media3 1.8.1's AOSP-derived `LatmReader`, decodes through `MediaCodec`, and streams PCM through `AudioTrack`. Audio is default-muted, background-muted, independently diagnosed, and cannot fail video. Explicit `cpresent=0`, shared audio/video UDP ports and more than two channels remain unsupported. Protocol, UDP session, Compose and real API 34 codec/output tests pass. The 1-30 FPS control caps rendered frames because Canon's RTP start payload has no encoder-FPS field. |
| CCAPI RTP H.264/AAC LATM Live View on iOS | Implemented when advertised | Physical iPhone validation required; the 2026-08-11 R6 Mark III camera-Wi-Fi probe advertised RTP but rejected start with HTTP 503 | Requires the same advertised Canon endpoints, a same-subnet camera-Wi-Fi IPv4 address, Wi-Fi-only `NWListener` UDP reception, bounded RFC 3550/RFC 6184 parsing, SPS/PPS plus keyframe gating, and native `AVSampleBufferVideoRenderer` display. When SDP separately advertises in-band `MP4A-LATM/48000` on a distinct port, iOS binds its audio port, performs bounded RFC 6416 marker/timestamp reassembly, extracts AAC-LC mono/stereo plus AudioSpecificConfig, decodes with `AVAudioConverter`, and plays through `AVAudioEngine`. Audio is default-muted, foreground-muted, queue bounded, independently diagnosed and failure-isolated from video; explicit `cpresent=0`, a shared audio/video port and unsupported codec/rate/topology remain unavailable. Core parser, session replacement, default state and real AAC decoder-fixture tests cover the path. AUTO cleans up and falls back through multipart to JPEG when video startup fails; Tap AF and Click WB fetch one detailed Canon frame for real coordinate geometry. The 1-30 FPS value is a display cap. |

## Wired And Other Platforms

| Capability | Status | Completion gate |
| --- | --- | --- |
| Android camera Wi-Fi routing | Implemented; device validation required | Camera HTTP and RTP sockets use the reachable Wi-Fi `Network`; refreshed diagnostics require `cameraRoute=WIFI_BOUND`, `cameraNetworkAvailable=true`, `systemDefaultTransport=CELLULAR`, `systemDefaultValidated=true`, and `wifiCellularCoexistence=true` before claiming coexistence. Record those fields plus a successful public-internet check on a physical phone |
| Android USB device and PTP-interface diagnostics | Implemented; device validation required | Canon device, permission, interface and endpoints recorded on Android |
| Android PTP container transport, session and DeviceInfo | Implemented; device validation required | Exact USB packet, buffered reads, transaction sequencing and dataset parsing are unit-tested; record a real R6 Mark III response |
| Android PTP storage and media | Implemented; device validation required | Standard storage/object operations, Canon EOS `AvailableShots (0xD11B)` remaining-count preference with standard-value fallback, bounded listing, advertised `GetThumb (0x100A)`, per-item JPEG/PNG preview through bounded `GetObject`, streaming downloads, capability-gated two-step `SendObjectInfo`/`SendObject` upload with exact ObjectInfo readback, and advertised `DeleteObject (0x100B)` are test-covered; validate remaining shots, thumbnails, preview, card behavior, upload/download, and deletion on R6 Mark III |
| Android PTP standard still capture | Implemented when advertised; device validation required | UI enables capture only when DeviceInfo advertises `InitiateCapture (0x100E)`; record the physical result |
| Android PTP battery and standard property control | Implemented when advertised; device validation required | Descriptor/value codecs and command/data writes are unit-tested; record battery, ISO, Tv, Av, WB and any advanced standard properties advertised by R6 Mark III |
| Android Canon EOS USB event synchronization | Implemented when the full remote/event sequence is advertised; device validation required | `SetRemoteMode (0x9114)`, `SetEventMode (0x9115)` and immediate `GetEvent (0x9116)` are capability-gated. Bounded cancellable polling, property/recording/storage/media change hints, authoritative status/capability/media-list refresh, external host-capture download/acknowledgement, empty polls, missing capability and event ownership across App capture/Bulb are automated-test covered. Record body-dial, recording, card and physical-shutter behavior on R6 Mark III and require `EVENT_POLLING` in observed session features. |
| Android Canon EOS USB lens status | Implemented after a valid property readback; device validation required | The pinned R6 Mark III snapshot exposes read-only `LensName (0xD1D8)`. Direct Android USB requests that exact property only when Canon remote/event mode and `RequestDevicePropValue (0x9127)` are advertised, accepts only a bounded NUL-terminated printable-ASCII event, and adds `LENS_STATUS` only after the camera returns it. A non-empty value becomes the mounted lens name; an explicitly returned empty value means no mounted lens. Missing, rejected, malformed or oversized responses remain unsupported rather than being inferred from model or firmware. Parser/backend tests cover mounted, unmounted and absent-readback paths; verify lens attach/detach events and diagnostics on R6 Mark III. |
| Android Canon EOS camera clock synchronization | Implemented when advertised; device validation required | The pinned R6 Mark III libgphoto2 snapshot exposes writable UTC and local camera-time actions. Direct Android USB prefers an actual `UTCTime (0xD17C)` event, falls back to `CameraTime (0xD113)`, writes current Unix seconds as UINT32 through `SetDevicePropValueEx (0x9110)`, and requires a same-property event within ten seconds of the requested value before marking the feature observed. Exact packet, fallback, missing-property, rejected-write and mismatched-readback paths are unit-tested; verify the resulting body time and diagnostic observation on R6 Mark III. |
| Android Canon EOS remote release, half-press and Bulb | Implemented when the full operation sequence is advertised; device validation required | Remote/event preparation, `CaptureDestination (0xD11C)` routing, optional `PCHDDCapacity`, 1 MiB `GetPartialObject` host transfers, success-only `TransferComplete`, atomic app-private storage, RAW+JPEG draining, card fallback, half/full press with guaranteed release, captured-object polling, timeout/failure paths and teardown are unit-tested against the pinned libgphoto2 sequence. Bulb holds half/full press until the user stops, then releases full/half in reverse order and preserves active state when release fails. |
| Android Canon EOS capture destination | Implemented when both destinations are executable; device validation required | Photo settings expose Phone/Memory card only when the camera advertises host and card `0xD11C` values and the complete host-transfer/capacity path exists. Exact writes, localized raw-value callbacks, phone media completion, card selection, rejected writes, missing capability and pre-shutter capacity failure are automated-test covered |
| Android Canon EOS active recording card | Implemented for an advertised writable dual-card state; device validation required | The pinned R6 Mark III snapshot exposes `CurrentStorage (0xD11E)` as a writable UINT32 and reports separate CFe/SD storage IDs. Photo settings expose a recording-card selector only when the current value belongs to at least two writable IDs returned by that live session's `GetStorageIDs`/`GetStorageInfo`; IDs are never hardcoded. The backend re-reads the cards before every write, rejects removed/unknown values, sends the exact 4-byte ID through `SetDevicePropValueEx`, and changes local state only after camera success. Dual/single/missing/stale/rejected paths, localized generic labels and the offline UI are test-covered. |
| Android Canon EOS native autofocus | Implemented when advertised; device validation required | Remote/event preparation plus `DoAf (0x9154)` and `AfCancel (0x9160)` are required as a pair; exact no-argument commands, bounded hold, start-failure cleanup, missing-pair fallback, and operation-independent capability gating are unit-tested |
| Android Canon EOS manual focus drive | Implemented when advertised; device validation required | `DriveLens (0x9155)` Near/Far values 1-3 are capability-gated, wired to localized UI and unit/UI-tested |
| Android Canon EOS Live View focus magnification | Implemented when advertised; device validation required | Requires active Live View plus advertised `Zoom (0x9158)`; only the libgphoto2-backed R6 Mark III values 1x and 5x are accepted. Exact operation/parameter tests, capability absence, inactive-session rejection, localized UI and offline preview are covered. This is not CCAPI PowerShot optical zoom. |
| Android Canon EOS JPEG Live View | Implemented when advertised; device validation required | EVF mode/output writes, `GetViewFinderData (0x9153)`, busy retry, bounded block parsing and in-memory JPEG delivery are unit-tested |
| Android Canon EOS ISO/Tv/Av/WB | Implemented when advertised; device validation required | `0xC189/0xC18A` event parsing, camera-provided option gating, libgphoto2 value mappings and `SetDevicePropValueEx (0x9110)` writes are unit-tested; verify each mode on R6 Mark III |
| Android Canon EOS movie start/stop | Implemented when advertised; device validation required | `EVFRecordStatus (0xD1B8)` is exposed only when camera events advertise both Card and None. The backend drains stale events, sends the exact `SetDevicePropValueEx` packet, and requires a matching Card/None event within three seconds before reporting the new state; exact packets, start/stop, missing capability, rejection, timeout and no-optimistic-update paths are unit-tested. |
| Canon EOS wired text metadata | Implemented when advertised/readable; device validation required | Android direct USB implements Owner (`0xD115`), Artist (`0xD1D0`), Copyright (`0xD1D1`) and Nickname (`0xD125`) only when property writes plus `RequestDevicePropValue (0x9127)` are advertised and each field returns a valid EOS string event. PC libgphoto2 exposes only writable `TEXT` widgets at the exact four paths. Both transports limit input to 255 printable ASCII bytes, permit empty values, force same-property/path exact readback after every write and reject missing/mismatched/rejected responses without optimistic state. Android, PC Web and iOS/Android Bridge clients share a typed text-setting contract with English/Traditional Chinese UI and dirty-draft preservation. Diagnostic evidence contains paths/keys only, never metadata values. Physical R6 Mark III write/restore validation remains required. |
| Android Canon EOS shooting mode | Implemented when advertised; device validation required | `AutoExposureMode (0xD105)`, UINT16 uses the pinned libgphoto2 table and camera-advertised values; exact writes, status display, English/Traditional Chinese labels, mode-context synchronization and prior-photo-mode restoration are test-covered |
| Android Canon EOS dedicated Photo/Video switch | Implemented when advertised; device validation required | The main selector prefers no-argument `MovieSelectSWOn (0x9133)` / `MovieSelectSWOff (0x9134)` only when both operations and binary `FixedMovie (0xD1C2)` event state are present. Matching post-command readback is required within three seconds; exact commands, missing pair/state, rejection, timeout, no optimistic state and existing English/Traditional Chinese UI behavior are test-covered. |
| Android Canon EOS AF/drive/metering/picture-style vendor settings | Implemented when advertised; device validation required | Focus mode (`0xD108`), Continuous AF (`0xD1C9`), AF method (`0xD1BA`), Drive mode (`0xD106`), Metering (`0xD107`), Picture Style (`0xD110`) and Movie Servo AF (`0xD179`) use pinned libgphoto2 types/mappings; controls appear only for camera-advertised values, exact writes and rejection paths are unit-tested; English/Traditional Chinese labels are display-only and writes retain the raw camera value |
| Android Canon EOS exposure/color/bracketing vendor settings | Implemented when advertised; device validation required | Exposure compensation (`0xD104`, UINT8), color temperature (`0xD10A`, UINT32), WB shifts (`0xD10B/0xD10C`, INT32), color space (`0xD10F`, UINT16), High ISO noise reduction (`0xD178`, UINT16), and AEB (`0xD1D9`, UINT16) use the pinned libgphoto2 tables and R6 Mark III advertised lists; signed decoding, exact writes, capability gating, localization, and mode filtering are unit-tested |
| Android Canon EOS aspect ratio, power-zoom speed and auto power-off | Implemented when advertised; device validation required | Aspect ratio (`0xD194`, UINT32), power-zoom speed (`0xD149`, UINT32) and Auto Power Off (`0xD114`, UINT32) use pinned libgphoto2 mappings and exact writes. Auto Power Off intersects camera-advertised values with Canon's documented 15 sec.–30 min. plus Disable choices; the `0xFFFFFFFF` sentinel remains diagnostic-only. Single-option controls stay out of the normal UI while remaining in diagnostics |
| Android Canon EOS Auto Lighting Optimizer | Implemented when advertised; device validation required | `AloMode (0xD1C1)` uses libgphoto2's `Generic32` table and UINT32 EOS event parsing. Android writes only the exact camera-advertised intersection of the pinned Standard/Low/High/Off, manual-exposure-disabled, and x1/x2/x3 values. The current R6 Mark III snapshot advertises only `x3`, so that state remains visible in diagnostics but no non-actionable control is shown. English/Traditional Chinese display labels never alter raw writes. |
| Android Canon EOS image quality | Implemented when advertised; device validation required | Generic (`0xD120`), CF/CFexpress (`0xD121`) and SD (`0xD122`) ImageFormat events use bounded one/two-entry parsing; RAW/cRAW/JPEG labels, 28/44-byte writes, missing-capability, malformed-event and rejected-write paths are unit-tested against pinned libgphoto2 behavior and the R6 Mark III snapshot |
| Android Canon EOS remaining vendor settings | Research; structural diagnostics implemented | Unknown Canon `0xC189`/`0xC18A` property events are retained as at most 64 deduplicated code/event records containing only block/payload lengths and optional declared list type/count. Values, raw bytes, strings and custom-function contents are never recorded. Pinned R6 Mark III evidence leaves `customfuncex`, focus area, bracket mode, depth-of-field preview, UI lock, popup flash, flash state, one-shot RAW, EVF output/mode, exposure-mode dial and zoom position opaque, single-valued, internal, risky or without a reliable range/readback contract; none is exposed as a normal control until separately proven. |
| Android Canon EOS USB Touch AF | Implemented when advertised; device validation required | Requires complete Canon Live View, a balanced autofocus path and camera-advertised `TouchAfPosition (0x915B)`. The parser reads sensor width/height only from bounded viewfinder block `0x0E`; normalized UI coordinates become integer sensor coordinates for parameters `[3, x, y]`, followed by balanced AF start/cancel. Missing geometry, invalid coordinates and rejected commands send no false success. The pinned R6 Mark III snapshot does not advertise `0x915B`, so support on that body remains explicitly unconfirmed until a physical DeviceInfo and tap result are recorded. |
| Android Canon EOS USB Click White Balance | Implemented when advertised; device validation required | Requires complete Canon Live View and camera-advertised `ClickWB (0x9157)`, whose pinned libgphoto2 definition takes exactly `[x, y]`. The backend uses only bounded sensor width/height from viewfinder block `0x0E`; inactive Live View, invalid coordinates, missing geometry and rejected commands produce no observed success. Pinned R6, R6 Mark II and R7 snapshots advertise `0x9157`, while the R6 Mark III snapshot does not, so support on the priority body remains explicitly unconfirmed until its physical DeviceInfo proves otherwise. |
| Desktop Bridge HTTP service | Implemented; device validation required | FastAPI service, loopback/Bearer security, stable errors, sessions, streaming responses and contract tests pass |
| Windows standalone Desktop Bridge | Implemented; release validation required | A pinned PyInstaller build embeds the Python runtime, PyAV/FFmpeg and package static UI in one Windows x64 console executable. It opens only a ready loopback UI, preserves the LAN token gate without a process-list-visible token argument, carries product version resources, and is launched in Windows CI to verify the root UI, CSP, JavaScript, health version and all three registered camera engines. `edsdk` is normally unavailable because no provider or Canon material is bundled. USB still requires a system gphoto2 path. |
| libgphoto2 desktop adapter | Implemented; device validation required | Native gphoto2 is preferred; Windows automatically uses a no-shell `wsl.exe [--distribution ...] --exec gphoto2` argument prefix when needed, with explicit diagnostics for missing distro/package/usbipd and a read-only doctor. Discovery, dynamic capabilities/settings, R6 Mark III remaining shots from read-only `availableshots` with storage-summary fallback, WB shifts/per-card image quality/aspect ratio/power-zoom/Auto Power Off/Auto Lighting Optimizer/Capture Target, bounded writable TEXT metadata with forced exact same-path readback, AF-ON through paired writable `autofocusdrive`/`autofocuscancel` actions with balanced half-press fallback, explicit half-press, recording, focus drive, writable `eoszoom` 1x/5x Live View focus magnification, cancellable persistent `--capture-movie --stdout` MJPEG with a 1-30 FPS output cap, control-safe restart and 5 FPS `--capture-preview` fallback, media listing/download, per-item bounded JPEG/PNG preview, thumbnails, exact-file deletion and subprocess timeout are test-covered. ALO accepts only pinned known choices and, like other non-core choice controls, requires at least two safe runtime values. Event synchronization is advertised only after a real `--wait-event=1ms` probe, preserves probe events, uses bounded 250 ms waits, suppresses stopped in-flight results, and drives authoritative status/capability plus open-media refresh in PC, Android, and iOS clients; it pauses instead of interrupting persistent Live View or Bulb. Memory-card capture uses trigger/image ability; advertised Internal RAM/SDRAM uses `--capture-image-and-download`, unique RAW+JPEG-safe names, same-volume staging, atomic promotion into a persistent opaque-ID host media store, bounded local previews/thumbnails, streamed download, exact deletion, partial-file cleanup, absolute configurable storage and no-shell WSL path mapping. RAW/HEIF/video do not advertise wired preview. Camera-resident thumbnail/delete remain ability-gated. Validate both Capture Targets, metadata write/restore, ALO, body-dial/file events, media preview, magnification, sustained rate and control interruptions on R6 Mark III |
| libgphoto2 camera clock synchronization | Implemented when an action/readback pair is advertised; device validation required | The pinned R6 Mark III snapshot exposes writable `syncdatetimeutc` and `syncdatetime` actions plus DATE-valued `datetimeutc` and `datetime` widgets. The Bridge prefers the UTC pair, forces `--list-all-config` after the action, and marks `CAMERA_CLOCK_SYNC` observed only when the same widget reports valid UINT32 Unix seconds within ten seconds of the operation interval. UTC/local fallback, action-only capability absence, rejected actions, mismatched readback and refresh failure are test-covered. Confirm the body menu and observed diagnostics on R6 Mark III. |
| libgphoto2 active recording card | Implemented for an advertised writable dual-card state; device validation required | The Bridge parses gphoto2's official `[Storage N]` output, obtains each PTP ID only from `basedir=/store_<id>`, converts its documented KB capacity to bytes and requires numeric `access=0`. A Photo-only `capturestorage` setting appears on PC, Android Bridge and iOS Bridge only when writable TEXT `storageid` has a valid current ID and at least two live writable cards. Writes map an advertised display label back to its runtime ID, strictly re-run config/storage discovery, reject removed/read-only/unknown cards, and send no hardcoded R6 Mark III ID. Official/summary parsing, duplicate labels, order changes, single/missing/read-only cards, stale refreshes and rejected writes are test-covered. |
| PC direct CCAPI engine | Implemented; device validation partly complete | Manual HTTP(S) origin and memory-only Basic Auth, advertised-operation discovery including Canon's documented developer-list fallback for the exact marker or a version-only root with zero valid commands, dynamic settings and controls including separately gated Auto Power Off and immediate camera sleep, bounded same-origin `?kind=thumbnail` thumbnails and `?kind=display` previews for JPEG/CR3 candidates under the advertised contents GET, and 1-30 FPS bounded JPEG polling. Live View retries the no-size form after HTTP 400, downgrades an invalid advertised `LARGE` start through `MEDIUM`/`SMALL`, removes only sizes physically rejected in the current session, conditionally retries `small` after first-frame HTTP 503, sends no unsupported cache query to `flipdetail`, and retries bounded transient multipart HTTP 503 startup races. Capability-gated RTP H.264 requires advertised `GET rtpsessiondesc`/`POST rtp`, a routed local IPv4 and PyAV. Empty/malformed developer lists never create capabilities. RTP video includes SDP validation, RFC 3550/RFC 6184 single NAL/STAP-A/FU-A handling, SPS/PPS/keyframe gating, exact UDP/HTTP cleanup, decode-to-JPEG output and AUTO fallback through multipart to polling. Advertised in-band `MP4A-LATM/48000` receives independent RFC 6416 reassembly, real PyAV/FFmpeg decode, bounded 48 kHz stereo PCM and authenticated long polling; explicit `cpresent=0` remains unsupported, and audio failure never invalidates video. Required browser E2E covers POST-only Live View, rejected RTP/multipart cleanup, size fallback, Canon 1.1 events, sleep disconnect and event cancellation. The 2026-08-12 R6 Mark III Wi-Fi pass validated firmware 1.1.0 discovery, 28 reversible setting writes/restores, status/lens/storage/temperature, AUTO and explicit JPEG repeatability, five rapid multipart restarts, `SMALL`/`MEDIUM`, `LARGE` downgrade, 6/15/30 requested versus 6.0/14.9/19.9 observed FPS, AF, balanced focus drive, center Tap AF, events, 500-item media traversal, metadata, thumbnail/display preview and an exact-length 9,313,518-byte in-memory download. RTP start still returned HTTP 503; recording/capture, media mutation, Click WB, clock write, sensor cleaning and sleep were intentionally not invoked. |
| PC control UI | Implemented; device validation required | Built into the bridge root URL; USB/CCAPI mode selection, capability-gated controls including Bulb start/stop with elapsed time and suspended frame polling, an in-viewfinder 1x/5x focus-magnification action, Auto/RTP/JPEG selection when multiple sources exist, a default-muted speaker control only for advertised/active RTP audio, authenticated binary Live View/PCM/downloads/thumbnails/display previews, WebAudio discontinuity recovery and teardown, visible cancellable media-transfer progress with no local path in diagnostics, requested/active source diagnostics, object-URL cleanup, confirmation-gated deletion, memory-only secrets, capability evidence in redacted diagnostics, English/Traditional Chinese, desktop/narrow browser interaction and overflow checks pass. Required browser workflows assert exact sanitized libgphoto2 commands and production Wireless CCAPI backend state for ISO, clock sync, balanced AF-ON, half-press, capture, decoded JPEG Live View, geometry-backed Tap AF/Click WB, media preview/delete, manual focus, recording, mode-gated Bulb, diagnostics and disconnect cleanup; the Wireless workflow additionally changes ISO and captures outside the UI, proving advertised Canon events refresh the exposure strip and open media view without manual refresh, while the USB workflow covers 1x/5x magnification and control-safe persistent MJPEG restart. Physical R6 Mark III validation remains required. |
| PC local UVC/HDMI preview input | Implemented; device validation required | The Bridge browser UI can select a local `videoinput`, open it with video-only `getUserMedia`, switch cleanly between camera Live View and local video while retaining the active camera-control session, stop every media track on source/device/disconnect/unload changes, and report rolling FPS plus non-identifying track settings. Device IDs and labels are memory-only and excluded from diagnostics. Monitoring assists work on the selected video frame; Canon coordinate Tap AF, Click White Balance, focus magnification and current libgphoto2 focus drive remain disabled because local video provides no proven mapping to camera Live View coordinates/state. The browser workflow test decodes a valid Bridge JPEG while exercising Local Video to Camera and back to Local Video, in addition to module, static-contract and responsive checks; physical R6 Mark III UVC and HDMI capture-card validation remains. |
| Android Desktop Bridge client | Implemented; device validation required | Bearer token is memory-only; discovery, camera selection, session lifecycle, bounded capability-evidence parsing, typed choice/text settings including metadata, controls including Live View focus magnification, JPEG frames, bounded authenticated media thumbnails/display previews, media streaming/deletion, diagnostics redaction and Live View request clamping are test-covered. Older Bridge responses with missing or invalid profile enums infer canonical family/priority from the returned camera model; unknown models remain `UNKNOWN` / `RESEARCH` and are never presented as R6 Mark III. |
| iOS Desktop Bridge client | Implemented; device validation required | Native Swift service validation, memory-only Bearer token, USB camera discovery/selection, session lifecycle, typed choice/text settings including metadata, dynamic capabilities, capture/record/focus and Live View focus magnification, bounded JPEG frames/thumbnails/display previews, file-backed media download/deletion and secret-redacted diagnostics are contract-tested on macOS CI |
| Optional Canon EDSDK provider | SDK-neutral boundary implemented; real provider remains Research | The Bridge discovers exactly one separately installed Python entry-point provider, validates contract version and bounded metadata, reports safe unavailable/load health, aggregates engine-tagged cameras, routes the exact engine through PC/Android/iOS, closes invalid sessions, and prevents simultaneous gPhoto2/EDSDK USB ownership. Public releases contain no Canon binary, header, document, constant, ABI declaration, or working provider. Canon's application-delivered SDK contract and physical R6 Mark III behavior are still required before any EDSDK capability can be marked supported. See [edsdk-provider.md](edsdk-provider.md). |
| iOS CCAPI core | Implemented; device validation required | Native Swift package normalizes same-origin full-URL/relative-path discovery entries, follows Canon's documented developer-list fallback for the exact marker or a version-only root with zero valid commands, rejects an empty/malformed fallback without inferred capabilities, requires advertised writable settings and complete Live View lifecycles, parses Canon RTP SDP plus RFC 3550/RFC 6184 H.264, capability-gates commands including independent autofocus, coordinate Tap AF, Click White Balance, `drivefocus` Near/Far steps 1-3, timed Auto Power Off and separate immediate camera sleep, bounded `?kind=thumbnail` thumbnails, `?kind=display` previews for JPEG/CR3 candidates under the advertised contents GET and exact-path media deletion, records bounded discovery evidence, controls capture/record/focus, rejects text metadata downloads, redacts diagnostics, and is covered by macOS Swift tests |
| iOS camera-control app | Implemented; device validation required | iOS 17 SwiftUI app reaches either native CCAPI or Desktop Bridge sessions for capability-gated controls, JPEG or advertised RTP H.264 Live View, capture/record/focus, file-backed media transfer with URLSession byte progress/cancellation/incomplete-file cleanup, confirmed deletion, diagnostics and decoded-frame monitoring assists. RTP uses Wi-Fi-bound UDP listeners, native sample-buffer video, a separately advertised default-muted AAC-LATM monitor, AUTO/JPEG/RTP selection and an adjustable display cap. Bridge scan/selection, memory-only secrets, matching English/Traditional Chinese resources, offline preview, safe orientation behavior, app unit tests and eight iPhone Simulator UI workflows are CI-gated. The Simulator-preset workflow exercises decoded Live View and the full control/media subset. A separate HTTP-preset workflow carries explicit Canon intent through the production UI, requires discovery, versioned JPEG endpoints and Canon 1.1 event GET/DELETE, and proves that external ISO plus still-capture media changes update the UI without manual refresh. Operation revisions prevent an older event refresh from overwriting newer interactive state, and event-triggered media refresh waits for active media work; record physical direct-CCAPI and Bridge/R6 Mark III results. |
| iOS USB/PTP | Research | Public Apple API and a working physical-device path are demonstrated |

## Release Gate

A capability can move to **supported** only when:

1. The UI action reaches a real backend and unsupported transports do not expose an active control.
2. Request and response behavior is covered by deterministic tests.
3. Errors preserve the operation and transport context.
4. The target physical camera result is recorded when vendor-specific behavior is involved.
5. English and Traditional Chinese UI strings, accessibility labels, and offline preview states are present.
6. A fresh physical-device report lists the capability in `observedFeatures` after the user exercises it; an entry in `supported` alone is insufficient.
7. Committed physical-camera evidence is generated by `scripts/validation/verify_diagnostic_report.py`; required features must be advertised and observed, while camera-side physical effects require a separate `Operator-confirmed` entry. The source diagnostic report remains private and must pass schema, freshness, consistency, completeness, and privacy checks.

Canon's current public SDK page lists EOS R6 Mark III for CCAPI and describes remote settings, capture, and image retrieval. Canon's detailed API specification remains the conformance authority distributed through CAP; public reference implementations are corroborating evidence, not a substitute for physical R6 Mark III validation.
