[project]
name = "open-eos-control-simulator"
version = "0.1.0"
description = "Fake Canon camera simulator for Open EOS Control"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.8.0",
  "uvicorn[standard]>=0.30.0"
]

[project.optional-dependencies]
dev = ["ruff>=0.6.0"]
